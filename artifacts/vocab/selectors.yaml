# The closed body-selector + header-matcher vocabularies and the named
# ignore-set registry the `equivalent` assertion resolves — normative, never
# runner judgment.
body_selectors: [prefer_conditional, error_loose, result_set_body, negotiated, present, absent]
header_matchers: ["present", "present?", "absent", "negotiated", "latest-version-uid", "pattern:<regex>", "<literal>"]
ignore_sets:
  server_assigned:
    per_binding: true
    source: "ITS-REST overview Requests_and_responses (server-assigned attributes; membership enumerated per operation binding)"
  ctx_defaults:
    paths:
      - context/start_time
      - context/setting
      - composer
    source: "ITS-REST simplified_formats master06 (ctx defaults: time -> now(), setting -> openehr::238, composer defaults)"
universal_outcomes:
  unauthenticated:
    status: 401
    source: "ITS-REST overview Requests_and_responses (HTTP status codes: 401 unauthenticated) — route-table-wide, never per binding"
  forbidden:
    status: 403
    source: "ITS-REST overview Requests_and_responses (HTTP status codes: 403 forbidden) — route-table-wide, never per binding"
  unsupported_media:
    status: 415
    source: "ITS-REST overview Requests_and_responses (HTTP status codes: 415 Unsupported Media Type) — route-table-wide, never per binding"
