# EXTENSION realization of I_DEFINITION_ADL14.delete_archetype.
#
# NO RELEASED WIRE EXISTS (register AMB-41, re-derived at the #623 wave and
# unchanged): the ITS-REST docs text names no ADL 1.4 archetype resource and
# the released definition OAS declares /definition/template/adl1.4* alone.
#
# WHAT THIS BINDING DRIVES INSTEAD: the route this product serves of its own
# design (DELETE /definition/archetype/adl1.4/{archetype_id}), declared in the `adl14-archetype` family of
# vocab/wire_surface.yaml served_extensions. NO openEHR SPEC GOVERNS IT — our
# own design/extension. It gates the Adl14ArchetypeProvisioning CAPABILITY
# verdict only, never ITS-REST wire conformance.
sm_operation: I_DEFINITION_ADL14.delete_archetype
its: its-rest
extension:
  family: adl14-archetype
  reason: >-
    ITS-REST 1.1.0 provisions operational templates only, so the SM ADL 1.4
    archetype operations have no released wire; this product serves them on
    its own /definition/archetype/adl1.4 routes, which is what the cases
    drive (owner adjudication 2026-07-29: full implementation over an
    excused claim — the book-CORE required marker drops with the register
    derivation, since an extension realization may never be required).
  source: >-
    SM docs/UML/classes/i_definition_adl14.adoc (archetype provisioning); ITS-REST
    specifications/operations (definition_template_adl1.4_* only, no archetype path anywhere);
    ITS-REST specifications/docs/definition (no mention); no openEHR spec governs this — our own
    design/extension
  ambiguity: AMB-41
request:
  method: DELETE
  path: /definition/archetype/adl1.4/{archetype_id}
outcomes:
  deleted: { status: 204 }
  not_found: { status: 404 }
