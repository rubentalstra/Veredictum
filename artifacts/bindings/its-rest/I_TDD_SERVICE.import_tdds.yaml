# ITS-REST 1.1.0 exposes NO TDD API — unrealized so the master13 import_tdds
# cases are not-applicable with citation (AMB-34).
sm_operation: I_TDD_SERVICE.import_tdds
its: its-rest
unrealized:
  reason: >-
    ITS-REST 1.1.0 has no TDD (Template Data Document) API: there are no tdd_*
    operations in specifications/operations. Bulk TDD import has no wire.
  source: "ITS-REST specifications/operations (no tdd operations) vs SM docs/UML/classes/i_tdd_service.adoc (import_tdds)"
  ambiguity: AMB-34
