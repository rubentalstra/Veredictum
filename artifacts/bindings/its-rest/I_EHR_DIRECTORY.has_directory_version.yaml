# Wire realization of I_EHR_DIRECTORY.has_directory_version for ITS-REST 1.1.0.
# The SM has_directory_version(ehr_id, a_version_uid) returns Boolean; ITS-REST
# 1.1.0 has no dedicated directory-version-existence probe, so — mirroring
# I_EHR_COMPOSITION.has_composition — it is realized as
# GET /ehr/{ehr_id}/directory/{version_uid}: 200 = the version exists (TRUE),
# 404 = no such version, or the EHR does not exist (FALSE / error). The boolean
# is carried by the ok/not_found outcome kind (no dedicated existence endpoint in ITS-REST 1.1.0).
# Sources: specifications/operations/directory_get_by_version_id.yaml (200/404),
# parameters/path/version_uid.yaml, responses/200_FOLDER_retrieved.yaml,
# 404_directory_unknown_ehr_id_or_no_version_uid_or_no_path.yaml.
sm_operation: I_EHR_DIRECTORY.has_directory_version
its: its-rest
request:
  method: GET
  path: /ehr/{ehr_id}/directory/{version_uid}
formats: [canonical-json, canonical-xml]
outcomes:
  ok: { status: 200, headers: { Content-Type: negotiated } }   # the version exists (TRUE)
  not_found: { status: 404 }                                   # no such version, or absent EHR (FALSE / error)
