# ITS-REST 1.1.0 exposes NO message/extract API — an explicit unrealized
# declaration so the master13 export_ehrs cases are not-applicable WITH
# CITATION on this ITS (never silently absent), activating when a MESSAGE API
# release adds the endpoint (the delete_opt pattern).
sm_operation: I_EHR_EXTRACT_SERVICE.export_ehrs
its: its-rest
unrealized:
  reason: >-
    ITS-REST 1.1.0 has no MESSAGE/EHR-Extract API: there are no
    message_*/extract_* operations in specifications/operations. Whole-EHR
    export as EXTRACTs has no wire.
  source: "ITS-REST specifications/operations (no message/extract operations) vs SM docs/UML/classes/i_ehr_extract_service.adoc (export_ehrs)"
  ambiguity: AMB-34
