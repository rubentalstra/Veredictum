# ITS-REST 1.1.0 surfaces no wire for I_ADMIN_SERVICE.composition_version_count
# — unrealized so the master12 cases are not-applicable with citation (AMB-33).
sm_operation: I_ADMIN_SERVICE.composition_version_count
its: its-rest
unrealized:
  reason: >-
    No admin activity-report operation exists in ITS-REST 1.1.0 (only
    admin_ehr_delete / admin_ehr_delete_all); there is no endpoint returning a
    COMPOSITION version count.
  source: "ITS-REST specifications/operations (admin_ehr_delete, admin_ehr_delete_all only) vs SM docs/UML/classes/i_admin_service.adoc (composition_version_count)"
  ambiguity: AMB-33
