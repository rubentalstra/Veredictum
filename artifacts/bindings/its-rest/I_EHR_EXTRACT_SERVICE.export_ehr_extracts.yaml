# ITS-REST 1.1.0 exposes NO message/extract API — unrealized so the master13
# export_ehr_extracts cases are not-applicable with citation (AMB-34).
sm_operation: I_EHR_EXTRACT_SERVICE.export_ehr_extracts
its: its-rest
unrealized:
  reason: >-
    ITS-REST 1.1.0 has no MESSAGE/EHR-Extract API: there are no
    message_*/extract_* operations in specifications/operations. Exporting an
    EXTRACT for one or more EHRs by EXTRACT_SPEC has no wire.
  source: "ITS-REST specifications/operations (no message/extract operations) vs SM docs/UML/classes/i_ehr_extract_service.adoc (export_ehr_extracts)"
  ambiguity: AMB-34
