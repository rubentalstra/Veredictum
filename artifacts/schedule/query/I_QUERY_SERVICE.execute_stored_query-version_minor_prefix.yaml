# A `{major}.{minor}` PREFIX selector narrows to that minor line — it does NOT
# fall through to a higher minor of the same major.
#
# Ground (ITS-REST docs text, the released oracle).
# docs/query/Qualified_query_name.md: "When only a partial `version` pattern is
# supplied, or when `version` is not supplied at all, the system must use the
# latest `version` with the supplied prefix - i.e. if only `major` or
# `major.minor` is used, then the latest query version matching supplied prefix
# will be used." This case is the `{major}.{minor}` branch: with 1.0.0 and 1.2.0
# both stored, the selector `1.0` matches only the 1.0 line, so it must resolve
# to 1.0.0 — the sibling `{major}` case pins that the same name at selector `1`
# resolves to 1.2.0 instead.
#
# HOW THE EXECUTED VERSION IS OBSERVED. The RESULT_SET carries no version field
# (register AMB-103), so each definition projects the same column under a
# different `AS` alias and the alias surfaces as the RESULT_SET column name
# (docs/query/Response.md §"RESULT_SET response").
id: I_QUERY_SERVICE.execute_stored_query-version_minor_prefix
kind: functional
component: QUERY
sm_operation: I_QUERY_SERVICE.execute_stored_query
capabilities: [QueryApi]
profiles: [STANDARD]
test_purpose: >-
  A stored query addressed by a `{major}.{minor}` selector executes the latest
  version within that minor line, not a higher minor of the same major.
description: "Stored-query execution at a {major}.{minor} version prefix stays inside that minor"
spec_refs:
  - "SM openehr_platform §I_QUERY_SERVICE.execute_stored_query"
  - "SM openehr_platform §I_DEFINITION_QUERY.store_query"
  - "ITS-REST query Qualified_query_name.md §version identifier"
  - "ITS-REST query Response.md §RESULT_SET response"
  - "QUERY AQL §SELECT, §FROM, §CONTAINS"
applies: { rm: ">=1.0.2", aql: ">=1.1" }
requires:
  server: any
  templates: [cnf.opt.blood_pressure]
  ehr: { commits: none }                      # mints ${ehr_id}
  commit: [cnf.set.query-bp-10]               # generated: 10 BP compositions
flow:
  - step: 1
    call: I_DEFINITION_QUERY.store_query
    variant: version
    with:
      qualified_query_name: "org.openehr.cnf::version_minor"
      version: "1.0.0"
      query: "SELECT c/uid/value AS v_1_0_0 FROM EHR e CONTAINS COMPOSITION c"
    expect: stored
  - step: 2
    call: I_DEFINITION_QUERY.store_query
    variant: version
    with:
      qualified_query_name: "org.openehr.cnf::version_minor"
      version: "1.2.0"
      query: "SELECT c/uid/value AS v_1_2_0 FROM EHR e CONTAINS COMPOSITION c"
    expect: stored
  - step: 3
    call: execute_stored_query
    variant: version                          # GET /query/{name}/1.0
    with:
      qualified_query_name: "org.openehr.cnf::version_minor"
      version: "1.0"
      ehr_id: "${ehr_id}"                     # single-EHR execution context
      fetch: 100                              # fetch always explicit
    expect: ok
    assert:
      - assert: result_set
        match: set                            # no ORDER BY: bag equality (QUERY master03 §ORDER BY)
        rows: { from: "${ds:cnf.set.query-bp-10#all_uids_asc}" }
        columns: [{ name: v_1_0_0 }]          # the only version inside the 1.0 line
