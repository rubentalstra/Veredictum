# The `openehr-ehr-id` REQUEST HEADER, alone, scopes a POST ad-hoc execution to
# one EHR — the sibling of the GET case, isolating the same behaviour on the
# body-carrying method. Ground (ITS-REST docs text, the released oracle):
# docs/query/Request.md §"About the ehr_id parameter" — "Depending on the needs,
# clients MAY supply it as a query parameter `ehr_id` or alternatively as a
# request header named `openehr-ehr-id`". The POST body carries only the query
# and its parameters (§"GET vs POST": "a mostly self-documented AQL query in a
# request body" — `q` + `query_parameters`), so the header is the only
# alternative scope form available on POST.
#
# The AQL is deliberately UNSCOPED (Query_types.md §"Population queries"), so
# the header is the ONLY thing narrowing the result: a server that ignores it
# answers with every EHR's compositions and the bag equality below fails.
#
# THE DISCRIMINATION IS TWO-EHR AND DETERMINISTIC: the flow mints a SECOND EHR
# and commits a COMPOSITION into it before executing, so the server holds
# content in two EHRs and the unscoped AQL selects over both. The bag equality
# below names exactly the first EHR's ten uids, so a server that ignores the
# header returns eleven rows and the row fails on the extra one — the
# expectation cannot be met by an unscoped answer. (A single-EHR corpus could
# only fail a server that returned FEWER rows.) The second EHR's content comes
# from a different template on purpose: it is visible to the unscoped query
# and belongs to no view of the scoped set.
id: I_QUERY_SERVICE.execute_ad_hoc_query-ehr_id_header_post
kind: functional
component: QUERY
sm_operation: I_QUERY_SERVICE.execute_ad_hoc_query
capabilities: [AqlBasic, QueryApi]
profiles: [STANDARD]
test_purpose: >-
  A POST ad-hoc execution whose EHR scope is supplied ONLY as the
  `openehr-ehr-id` request header returns exactly that EHR's compositions —
  the header is honoured identically on the body-carrying method.
description: "POST ad-hoc AQL scoped by the openehr-ehr-id request header alone"
spec_refs:
  - "SM openehr_platform §I_QUERY_SERVICE.execute_ad_hoc_query"
  - "ITS-REST query Request.md §About the ehr_id parameter"
  - "ITS-REST query Request.md §GET vs POST"
  - "ITS-REST query Query_types.md §Single EHR queries, §Population queries"
  - "QUERY AQL §CONTAINS, §Identified Paths"
applies: { rm: ">=1.0.2", aql: ">=1.1" }
requires:
  server: any
  templates: [cnf.opt.blood_pressure, cnf.opt.minimal_event]
  ehr: { commits: none }                    # mints ${ehr_id}
  commit: [cnf.set.query-bp-10]             # generated: 10 BP compositions
data_sets: [cnf.composition.minimal_event.v1]
flow:
  - step: 1                                 # a SECOND EHR, so the scoped read has something to exclude
    call: I_EHR_SERVICE.create_ehr
    expect: created
    capture: { second_ehr_id: created.ehr_id }
  - step: 2                                 # content in the second EHR — an unscoped answer would carry it
    call: I_EHR_COMPOSITION.create_composition
    with: { ehr_id: "${second_ehr_id}", composition: "${ds:cnf.composition.minimal_event.v1}" }
    expect: created
  - step: 3
    call: execute_ad_hoc_query
    variant: post_ehr_id_header             # POST /query/aql, scope via openehr-ehr-id
    with:
      ehr_id: "${ehr_id}"                   # travels in the header, not the query string
      q: >-
        SELECT c/uid/value AS uid
        FROM EHR e CONTAINS COMPOSITION c
      fetch: 100                            # fetch always explicit
    expect: ok
    assert:
      - assert: result_set
        match: set                          # no ORDER BY: bag equality (QUERY master03 §ORDER BY)
        rows: { from: "${ds:cnf.set.query-bp-10#all_uids_asc}" }
        columns: [{ name: uid }]
