# OFFICIAL master16 test case (rebuilt 2026-07-24, catalogue audit D3).
# Verdicts recomputed against AOM1.4 C_ATTRIBUTE.existence and the RM
# OBSERVATION / CARE_ENTRY classes. Realization (OBSERVATION synth family):
# "data absent" is realized at the event level (the committed EVENT omits its
# RM-mandatory data) — the RM/schema rejection fires either way.
id: CONT-OBS-state_ex_mand-protocol_ex_opt
kind: content
component: CONTENT
rm_class: OBSERVATION
status: active
capabilities: [ArchetypeValidation]
profiles: [CORE]
test_purpose: >-
  OBSERVATION state existence 1..1, protocol existence 0..1: a committed
  OBSERVATION is rejected when its RM-mandatory data is absent or when the
  archetype-mandated state is absent; protocol may be freely present or absent.
description: "OBSERVATION data (RM mandatory) + archetype-mandated state, protocol optional"
spec_refs:
  - "CNF platform_test_schedule master16 §Test Case CONT-OBS-state_ex_mand-protocol_ex_opt"
  - "AM AOM1.4 §C_ATTRIBUTE (existence) — whether the attribute field must be present"
  - "RM ehr §OBSERVATION (data: HISTORY, existence 1..1; state: HISTORY, existence 0..1)"
  - "RM ehr §CARE_ENTRY (protocol: ITEM_STRUCTURE, existence 0..1)"
applies: { rm: ">=1.0.2" }
constraint_context:
  # C_ATTRIBUTE.existence: state tightened to 1..1, protocol 0..1 (RM default).
  template: cnf.tpl.ecc_observation_state_protocol_existence
  path: "/content[openEHR-EHR-OBSERVATION.minimal.v1]"
  constraint_columns: ["state_existence", "protocol_existence"]
decision_table:
  columns: [state_existence, protocol_existence, data_committed, state_committed, protocol_committed, expected, violates]
  rows:
    - ["mandatory", "optional", "absent", "absent", "absent", rejected, ["rm_schema: OBSERVATION.data is mandatory (existence 1..1)", "constraint(C_ATTRIBUTE.existence): state is mandated by the archetype (existence 1..1) but absent"]]
    - ["mandatory", "optional", "absent", "absent", "present", rejected, ["rm_schema: OBSERVATION.data is mandatory (existence 1..1)", "constraint(C_ATTRIBUTE.existence): state is mandated by the archetype (existence 1..1) but absent"]]
    - ["mandatory", "optional", "absent", "present", "absent", rejected, ["rm_schema: OBSERVATION.data is mandatory (existence 1..1)"]]
    - ["mandatory", "optional", "absent", "present", "present", rejected, ["rm_schema: OBSERVATION.data is mandatory (existence 1..1)"]]
    - ["mandatory", "optional", "present", "absent", "absent", rejected, ["constraint(C_ATTRIBUTE.existence): state is mandated by the archetype (existence 1..1) but absent"]]
    - ["mandatory", "optional", "present", "absent", "present", rejected, ["constraint(C_ATTRIBUTE.existence): state is mandated by the archetype (existence 1..1) but absent"]]
    - ["mandatory", "optional", "present", "present", "absent", accepted, []]
    - ["mandatory", "optional", "present", "present", "present", accepted, []]
