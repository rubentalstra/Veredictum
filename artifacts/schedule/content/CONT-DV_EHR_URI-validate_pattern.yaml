id: CONT-DV_EHR_URI-validate_pattern
kind: content
component: CONTENT
rm_class: DV_EHR_URI
capabilities: [ArchetypeValidation]
profiles: [CORE]
test_purpose: "A committed DV_EHR_URI is accepted iff its value matches the C_STRING.pattern constraint."
description: "DV_EHR_URI against a C_STRING.pattern (ehr://.*)"
spec_refs:
  - "CNF platform_test_schedule master17.7 \u00a7CONT-DV_EHR_URI-validate_pattern"
  - "RM data_types \u00a7DV_EHR_URI"
  - "AM AOM1.4 \u00a7C_STRING"
constraint_context:
  template: cnf.tpl.dv_ehr_uri_c_string
  path: "/content[openEHR-EHR-OBSERVATION.minimal.v1]/data/events/data/items[at0001]/value"
decision_table:
  columns: ["value", "C_STRING.pattern", "expected", "violates"]
  rows:
    - ["xyz", "ehr://.*", "rejected", ["constraint(C_STRING.pattern): value does not match the pattern"]]
    - ["https://cabolabs.com", "ehr://.*", "rejected", ["constraint(C_STRING.pattern): value does not match the pattern"]]
    - ["ehr://CLOUD_EHRSERVER/89c0752e-0815-47d7-8b3c-b3aaea2cea7a", "ehr://.*", "accepted", []]
