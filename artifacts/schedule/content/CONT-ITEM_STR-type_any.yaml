# OFFICIAL master16 test case (rebuilt 2026-07-24, catalogue audit D4): the
# 2026-07-21 claim that master16 carries no test-case headings was false.
# Verdicts recomputed against AOM1.4 C_OBJECT.rm_type_name and the RM
# ITEM_STRUCTURE hierarchy (abstract ITEM_STRUCTURE; ITEM_TREE / ITEM_LIST /
# ITEM_TABLE / ITEM_SINGLE subtypes; each committed instance is a well-formed
# minimal instance of its subtype — ITEM_SINGLE carries its mandatory item).
id: CONT-ITEM_STR-type_any
kind: content
component: CONTENT
rm_class: ITEM_STRUCTURE
status: active
capabilities: [ArchetypeValidation]
profiles: [CORE]
test_purpose: >-
  ITEM_STRUCTURE is any ITEM_STRUCTURE subtype: an archetype slot constrained
  to the abstract ITEM_STRUCTURE class accepts all four subtypes at runtime.
description: "EVALUATION.data object type against an open (abstract ITEM_STRUCTURE) archetype slot"
spec_refs:
  - "CNF platform_test_schedule master16 §Test Case CONT-ITEM_STR-type_any"
  - "AM AOM1.4 §C_OBJECT (rm_type_name) — the reference-model type a node constrains"
  - "RM data_structures §ITEM_STRUCTURE / §ITEM_TREE / §ITEM_LIST / §ITEM_TABLE / §ITEM_SINGLE (abstract ITEM_STRUCTURE + four subtypes)"
applies: { rm: ">=1.0.2" }
constraint_context:
  # C_OBJECT.rm_type_name = ITEM_STRUCTURE (abstract/open) on the
  # EVALUATION.data slot.
  template: cnf.tpl.ecc_item_structure_type_narrowing
  path: "/content[openEHR-EHR-EVALUATION.minimal.v1]/data"
  constraint_columns: ["slot_type"]
decision_table:
  columns: [slot_type, committed_type, expected, violates]
  rows:
    - ["ITEM_STRUCTURE", "ITEM_TREE", accepted, []]
    - ["ITEM_STRUCTURE", "ITEM_LIST", accepted, []]
    - ["ITEM_STRUCTURE", "ITEM_TABLE", accepted, []]
    - ["ITEM_STRUCTURE", "ITEM_SINGLE", accepted, []]
