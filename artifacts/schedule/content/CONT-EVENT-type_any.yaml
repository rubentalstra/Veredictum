# OFFICIAL master16 test case (rebuilt 2026-07-24, catalogue audit D6).
# Verdicts recomputed against AOM1.4 C_OBJECT.rm_type_name and the RM EVENT
# hierarchy (abstract EVENT; POINT_EVENT / INTERVAL_EVENT subtypes;
# INTERVAL_EVENT.width and .math_function are RM-mandatory and always carried
# by the committed instance).
id: CONT-EVENT-type_any
kind: content
component: CONTENT
rm_class: EVENT
status: active
capabilities: [ArchetypeValidation]
profiles: [CORE]
test_purpose: >-
  EVENT is any EVENT subtype: an archetype slot constrained to the abstract
  EVENT class accepts both POINT_EVENT and a well-formed INTERVAL_EVENT at
  runtime.
description: "HISTORY.events object type against an open (abstract EVENT) archetype slot"
spec_refs:
  - "CNF platform_test_schedule master16 §Test Case CONT-EVENT-type_any"
  - "AM AOM1.4 §C_OBJECT (rm_type_name) — the reference-model type a node constrains"
  - "RM data_structures §EVENT / §POINT_EVENT / §INTERVAL_EVENT (abstract EVENT; INTERVAL_EVENT.width 1..1, .math_function 1..1)"
applies: { rm: ">=1.0.2" }
constraint_context:
  # C_OBJECT.rm_type_name = EVENT (abstract/open) on the HISTORY.events slot.
  template: cnf.tpl.ecc_event_type_narrowing
  path: "/content[openEHR-EHR-OBSERVATION.minimal.v1]/data/events"
  constraint_columns: ["slot_type"]
decision_table:
  columns: [slot_type, committed_type, expected, violates]
  rows:
    - ["EVENT", "POINT_EVENT", accepted, []]
    - ["EVENT", "INTERVAL_EVENT", accepted, []]
