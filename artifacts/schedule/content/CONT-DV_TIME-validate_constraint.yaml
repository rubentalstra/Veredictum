id: CONT-DV_TIME-validate_constraint
kind: content
component: CONTENT
rm_class: DV_TIME
capabilities: [ArchetypeValidation]
profiles: [CORE]
test_purpose: "A committed DV_TIME is accepted iff every time component satisfies the C_TIME validity-kind flags (mandatory component present, prohibited component absent)."
description: "DV_TIME against C_TIME minute/second/millisecond/timezone validity constraints"
spec_refs:
  - "CNF platform_test_schedule master17.4 \u00a7CONT-DV_TIME-validate_constraint"
  - "RM data_types \u00a7DV_TIME"
  - "AM AOM1.4 \u00a7C_TIME"
applies: { rm: ">=1.0.2" }
constraint_context:
  template: cnf.tpl.dv_time_c_time
  path: "/content[openEHR-EHR-OBSERVATION.minimal.v1]/data/events/data/items[at0001]/value"
decision_table:
  columns: ["value", "minute_validity", "second_validity", "millisecond_validity", "timezone_validity", "expected", "violates"]
  rows:
    - ["T10", "mandatory", "mandatory", "mandatory", "mandatory", "rejected", ["constraint(minute_validity): the minute component does not satisfy its validity kind", "constraint(second_validity): the second component does not satisfy its validity kind", "constraint(millisecond_validity): the millisecond component does not satisfy its validity kind", "constraint(timezone_validity): the timezone component does not satisfy its validity kind"]]
    - ["T10", "mandatory", "mandatory", "mandatory", "optional", "rejected", ["constraint(minute_validity): the minute component does not satisfy its validity kind", "constraint(second_validity): the second component does not satisfy its validity kind", "constraint(millisecond_validity): the millisecond component does not satisfy its validity kind"]]
    - ["T10", "mandatory", "mandatory", "optional", "optional", "rejected", ["constraint(minute_validity): the minute component does not satisfy its validity kind", "constraint(second_validity): the second component does not satisfy its validity kind"]]
    - ["T10", "mandatory", "optional", "optional", "optional", "rejected", ["constraint(minute_validity): the minute component does not satisfy its validity kind"]]
    - ["T10", "optional", "optional", "optional", "optional", "accepted", []]
    - ["T10", "mandatory", "mandatory", "mandatory", "prohibited", "rejected", ["constraint(minute_validity): the minute component does not satisfy its validity kind", "constraint(second_validity): the second component does not satisfy its validity kind", "constraint(millisecond_validity): the millisecond component does not satisfy its validity kind"]]
    - ["T10", "mandatory", "mandatory", "prohibited", "prohibited", "rejected", ["constraint(minute_validity): the minute component does not satisfy its validity kind", "constraint(second_validity): the second component does not satisfy its validity kind"]]
    - ["T10", "mandatory", "prohibited", "prohibited", "prohibited", "rejected", ["constraint(minute_validity): the minute component does not satisfy its validity kind"]]
    - ["T10", "prohibited", "prohibited", "prohibited", "prohibited", "accepted", []]
    - ["T10:30", "mandatory", "mandatory", "mandatory", "mandatory", "rejected", ["constraint(second_validity): the second component does not satisfy its validity kind", "constraint(millisecond_validity): the millisecond component does not satisfy its validity kind", "constraint(timezone_validity): the timezone component does not satisfy its validity kind"]]
    - ["T10:30", "mandatory", "mandatory", "mandatory", "optional", "rejected", ["constraint(second_validity): the second component does not satisfy its validity kind", "constraint(millisecond_validity): the millisecond component does not satisfy its validity kind"]]
    - ["T10:30", "mandatory", "mandatory", "optional", "optional", "rejected", ["constraint(second_validity): the second component does not satisfy its validity kind"]]
    - ["T10:30", "mandatory", "optional", "optional", "optional", "accepted", []]
    - ["T10:30", "optional", "optional", "optional", "optional", "accepted", []]
    - ["T10:30", "mandatory", "mandatory", "mandatory", "prohibited", "rejected", ["constraint(second_validity): the second component does not satisfy its validity kind", "constraint(millisecond_validity): the millisecond component does not satisfy its validity kind"]]
    - ["T10:30", "mandatory", "mandatory", "prohibited", "prohibited", "rejected", ["constraint(second_validity): the second component does not satisfy its validity kind"]]
    - ["T10:30", "mandatory", "prohibited", "prohibited", "prohibited", "accepted", []]
    - ["T10:30", "prohibited", "prohibited", "prohibited", "prohibited", "rejected", ["constraint(minute_validity): the minute component does not satisfy its validity kind"]]
    - ["T10:30:47", "mandatory", "mandatory", "mandatory", "mandatory", "rejected", ["constraint(millisecond_validity): the millisecond component does not satisfy its validity kind", "constraint(timezone_validity): the timezone component does not satisfy its validity kind"]]
    - ["T10:30:47", "mandatory", "mandatory", "mandatory", "optional", "rejected", ["constraint(millisecond_validity): the millisecond component does not satisfy its validity kind"]]
    - ["T10:30:47", "mandatory", "mandatory", "optional", "optional", "accepted", []]
    - ["T10:30:47", "mandatory", "optional", "optional", "optional", "accepted", []]
    - ["T10:30:47", "optional", "optional", "optional", "optional", "accepted", []]
    - ["T10:30:47", "mandatory", "mandatory", "mandatory", "prohibited", "rejected", ["constraint(millisecond_validity): the millisecond component does not satisfy its validity kind"]]
    - ["T10:30:47", "mandatory", "mandatory", "prohibited", "prohibited", "accepted", []]
    - ["T10:30:47", "mandatory", "prohibited", "prohibited", "prohibited", "rejected", ["constraint(second_validity): the second component does not satisfy its validity kind"]]
    - ["T10:30:47", "prohibited", "prohibited", "prohibited", "prohibited", "rejected", ["constraint(minute_validity): the minute component does not satisfy its validity kind", "constraint(second_validity): the second component does not satisfy its validity kind"]]
    - ["T10:30:47.5", "mandatory", "mandatory", "mandatory", "mandatory", "rejected", ["constraint(timezone_validity): the timezone component does not satisfy its validity kind"]]
    - ["T10:30:47.5", "mandatory", "mandatory", "mandatory", "optional", "accepted", []]
    - ["T10:30:47.5", "mandatory", "mandatory", "optional", "optional", "accepted", []]
    - ["T10:30:47.5", "mandatory", "optional", "optional", "optional", "accepted", []]
    - ["T10:30:47.5", "optional", "optional", "optional", "optional", "accepted", []]
    - ["T10:30:47.5", "mandatory", "mandatory", "mandatory", "prohibited", "accepted", []]
    - ["T10:30:47.5", "mandatory", "mandatory", "prohibited", "prohibited", "rejected", ["constraint(millisecond_validity): the millisecond component does not satisfy its validity kind"]]
    - ["T10:30:47.5", "mandatory", "prohibited", "prohibited", "prohibited", "rejected", ["constraint(second_validity): the second component does not satisfy its validity kind", "constraint(millisecond_validity): the millisecond component does not satisfy its validity kind"]]
    - ["T10:30:47.5", "prohibited", "prohibited", "prohibited", "prohibited", "rejected", ["constraint(minute_validity): the minute component does not satisfy its validity kind", "constraint(second_validity): the second component does not satisfy its validity kind", "constraint(millisecond_validity): the millisecond component does not satisfy its validity kind"]]
    - ["T10:30:47Z", "mandatory", "mandatory", "mandatory", "mandatory", "rejected", ["constraint(millisecond_validity): the millisecond component does not satisfy its validity kind"]]
    - ["T10:30:47Z", "mandatory", "mandatory", "mandatory", "optional", "rejected", ["constraint(millisecond_validity): the millisecond component does not satisfy its validity kind"]]
    - ["T10:30:47Z", "mandatory", "mandatory", "optional", "optional", "accepted", []]
    - ["T10:30:47Z", "mandatory", "optional", "optional", "optional", "accepted", []]
    - ["T10:30:47Z", "optional", "optional", "optional", "optional", "accepted", []]
    - ["T10:30:47Z", "mandatory", "mandatory", "mandatory", "prohibited", "rejected", ["constraint(millisecond_validity): the millisecond component does not satisfy its validity kind", "constraint(timezone_validity): the timezone component does not satisfy its validity kind"]]
    - ["T10:30:47Z", "mandatory", "mandatory", "prohibited", "prohibited", "rejected", ["constraint(timezone_validity): the timezone component does not satisfy its validity kind"]]
    - ["T10:30:47Z", "mandatory", "prohibited", "prohibited", "prohibited", "rejected", ["constraint(second_validity): the second component does not satisfy its validity kind", "constraint(timezone_validity): the timezone component does not satisfy its validity kind"]]
    - ["T10:30:47Z", "prohibited", "prohibited", "prohibited", "prohibited", "rejected", ["constraint(minute_validity): the minute component does not satisfy its validity kind", "constraint(second_validity): the second component does not satisfy its validity kind", "constraint(timezone_validity): the timezone component does not satisfy its validity kind"]]
    - ["T10:30:47.5Z", "mandatory", "mandatory", "mandatory", "mandatory", "accepted", []]
    - ["T10:30:47.5Z", "mandatory", "mandatory", "mandatory", "optional", "accepted", []]
    - ["T10:30:47.5Z", "mandatory", "mandatory", "optional", "optional", "accepted", []]
    - ["T10:30:47.5Z", "mandatory", "optional", "optional", "optional", "accepted", []]
    - ["T10:30:47.5Z", "optional", "optional", "optional", "optional", "accepted", []]
    - ["T10:30:47.5Z", "mandatory", "mandatory", "mandatory", "prohibited", "rejected", ["constraint(timezone_validity): the timezone component does not satisfy its validity kind"]]
    - ["T10:30:47.5Z", "mandatory", "mandatory", "prohibited", "prohibited", "rejected", ["constraint(millisecond_validity): the millisecond component does not satisfy its validity kind", "constraint(timezone_validity): the timezone component does not satisfy its validity kind"]]
    - ["T10:30:47.5Z", "mandatory", "prohibited", "prohibited", "prohibited", "rejected", ["constraint(second_validity): the second component does not satisfy its validity kind", "constraint(millisecond_validity): the millisecond component does not satisfy its validity kind", "constraint(timezone_validity): the timezone component does not satisfy its validity kind"]]
    - ["T10:30:47.5Z", "prohibited", "prohibited", "prohibited", "prohibited", "rejected", ["constraint(minute_validity): the minute component does not satisfy its validity kind", "constraint(second_validity): the second component does not satisfy its validity kind", "constraint(millisecond_validity): the millisecond component does not satisfy its validity kind", "constraint(timezone_validity): the timezone component does not satisfy its validity kind"]]
    - ["T10:30:47.5-03:00", "mandatory", "mandatory", "mandatory", "mandatory", "accepted", []]
    - ["T10:30:47.5-03:00", "mandatory", "mandatory", "mandatory", "optional", "accepted", []]
    - ["T10:30:47.5-03:00", "mandatory", "mandatory", "optional", "optional", "accepted", []]
    - ["T10:30:47.5-03:00", "mandatory", "optional", "optional", "optional", "accepted", []]
    - ["T10:30:47.5-03:00", "optional", "optional", "optional", "optional", "accepted", []]
    - ["T10:30:47.5-03:00", "mandatory", "mandatory", "mandatory", "prohibited", "rejected", ["constraint(timezone_validity): the timezone component does not satisfy its validity kind"]]
    - ["T10:30:47.5-03:00", "mandatory", "mandatory", "prohibited", "prohibited", "rejected", ["constraint(millisecond_validity): the millisecond component does not satisfy its validity kind", "constraint(timezone_validity): the timezone component does not satisfy its validity kind"]]
    - ["T10:30:47.5-03:00", "mandatory", "prohibited", "prohibited", "prohibited", "rejected", ["constraint(second_validity): the second component does not satisfy its validity kind", "constraint(millisecond_validity): the millisecond component does not satisfy its validity kind", "constraint(timezone_validity): the timezone component does not satisfy its validity kind"]]
    - ["T10:30:47.5-03:00", "prohibited", "prohibited", "prohibited", "prohibited", "rejected", ["constraint(minute_validity): the minute component does not satisfy its validity kind", "constraint(second_validity): the second component does not satisfy its validity kind", "constraint(millisecond_validity): the millisecond component does not satisfy its validity kind", "constraint(timezone_validity): the timezone component does not satisfy its validity kind"]]
