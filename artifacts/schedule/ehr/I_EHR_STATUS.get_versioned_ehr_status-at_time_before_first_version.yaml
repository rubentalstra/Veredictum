# The lower edge on the ENVELOPE route: below the first commit there is no
# VERSION to envelope.
#
# RM common versioned_object.adoc §Functions guards the accessor with
# `Pre: has_version_at_time (a_time)`; ITS-REST overview
# Requests_and_responses.md §HTTP status codes assigns the absent-target state
# its row: "404 | Not Found | The origin service did not find the target
# resource or is not willing to disclose that one exists".
#
# The queried instant is a FIXED literal in the extended ISO 8601 form the
# Resources.md §Datetime format MUST requires; the runner seeds a freshly
# composed, empty SUT, so every commit in the run is later than the year 2000.
id: I_EHR_STATUS.get_versioned_ehr_status-at_time_before_first_version
kind: functional
component: EHR
sm_operation: I_EHR_STATUS.get_versioned_ehr_status
capabilities: [EhrStatus]
exercises: [Versioning]
profiles: [CORE]
ambiguities: [AMB-70]
test_purpose: >-
  A versioned-EHR_STATUS version read for an instant earlier than the first
  committed version finds no version and is refused as not found.
description: "Get EHR_STATUS VERSION at time, before the first version"
spec_refs:
  - "RM common §versioned_object (version_at_time / has_version_at_time)"
  - "ITS-REST overview Requests_and_responses §HTTP status codes"
  - "SM openehr_platform §I_EHR_STATUS.get_versioned_ehr_status"
applies: { rm: ">=1.0.2", its_rest: ">=1.1.0" }
requires:
  server: any
  ehr: { commits: none }                 # mints ${ehr_id}
flow:
  - step: 1
    call: get_versioned_ehr_status
    variant: at_time
    with: { ehr_id: "${ehr_id}", a_time: "2000-01-01T00:00:00Z" }
    expect: not_found
