# Official master05 case. Cells are TBD placeholders ("xx" — AMB-38). valid_query is realized via store_query (SM
# Pre_valid_query): a syntactically invalid AQL text fails the formalism check
# and is rejected.
id: I_DEFINITION_QUERY.valid_query-invalid
kind: functional
component: DEFINITION_QUERY
sm_operation: I_DEFINITION_QUERY.valid_query
capabilities: [QueryProvisioning]
exercises: [AqlBasic]
profiles: [STANDARD]
test_purpose: "A syntactically invalid AQL query text is rejected (valid_query realized via store_query)."
description: "valid_query for an invalid AQL query (schedule cells TBD; SM-derived)"
spec_refs:
  - "SM openehr_platform §I_DEFINITION_QUERY.valid_query"
  - "CNF platform_test_schedule master05 §valid_query-invalid"
guards:
  - "master05 is TBD upstream: flows derived from the SM operation semantics and the stored-query Definitions API — CNF platform_test_schedule master05 (all cells xx/TBD); AMB-38"
applies: { rm: ">=1.0.2" }
requires: { server: empty }
flow:
  - step: 1
    call: store_query
    with:
      qualified_query_name: "org.openehr.cnf::invalid_query"
      query: "SELECT FROM WHERE CONTAINS"   # not a valid AQL instance of the formalism
      query_type: "AQL"
    expect: validation_failed            # invalid formalism -> Pre_valid_query fails, rejected
ambiguities: [AMB-18]
