# Official master05 case. Cells are TBD placeholders ("xx"). valid_query takes a_type (the formalism name/version — SM
# valid_query(a_query_text, a_type)); an unsupported formalism is rejected.
# Realized via store_query with an unsupported query_type.
id: I_DEFINITION_QUERY.valid_query-bad_formalism
kind: functional
component: DEFINITION_QUERY
sm_operation: I_DEFINITION_QUERY.valid_query
capabilities: [QueryProvisioning]
exercises: [AqlBasic]
profiles: [STANDARD]
test_purpose: "A query declared under an unsupported formalism (a_type) is rejected."
description: "valid_query for a bad formalism (schedule cells TBD; SM-derived)"
spec_refs:
  - "SM openehr_platform §I_DEFINITION_QUERY.valid_query"
  - "CNF platform_test_schedule master05 §valid_query-bad_formalism"
guards:
  - "flows derived from SM I_DEFINITION_QUERY operation semantics + released ITS-REST definition docs; CNF platform_test_schedule master05 is a stalled schedule stub (all cells xx/TBD) — a coverage guide only, not the authority"
applies: { rm: ">=1.0.2" }
requires: { server: empty }
flow:
  - step: 1
    call: store_query
    with:
      qualified_query_name: "org.openehr.cnf::bad_formalism"
      query: "//ehr[*]"                  # not AQL
      query_type: "XPATH"                # unsupported formalism (a_type)
    expect: validation_failed
ambiguities: [AMB-18]
