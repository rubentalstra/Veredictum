# Official master12 case (chapter TBD; NATURAL family id — see REQUIREMENTS.md).
# Realized via admin_ehr_delete; the SM Errors row names ehr_id_does_not_exist,
# wired as 404 (404_unknown_ehr_id.yaml).
id: I_ADMIN_SERVICE.physical_ehr_delete-delete_non_existing
kind: functional
component: ADMIN
sm_operation: I_ADMIN_SERVICE.physical_ehr_delete
capabilities: [PhysicalDeletion]
exercises: [AdminApi]
profiles: [OPTIONS]
test_purpose: "Physically deleting a non-existent EHR is rejected as not found and leaves the system unchanged."
description: "Physically delete a non-existent EHR"
spec_refs:
  - "SM openehr_platform §I_ADMIN_SERVICE.physical_ehr_delete"
  - "CNF platform_test_schedule master12 §physical_ehr_delete"
  - "ITS-REST admin §admin_ehr_delete (404_unknown_ehr_id)"
applies: { rm: ">=1.0.2" }
guards:
  - "ITS-REST Admin API is DEVELOPMENT-stage — admin.openapi.yaml carries x-status: DEVELOPMENT (not STABLE); guarded until the Admin API stabilises"
requires: { server: empty }
flow:
  - step: 1
    call: physical_ehr_delete
    on: admin                              # ADMIN-role principal (ixit)
    with: { ehr_id: "00000000-0000-0000-0000-000000000000" }
    expect: not_found
