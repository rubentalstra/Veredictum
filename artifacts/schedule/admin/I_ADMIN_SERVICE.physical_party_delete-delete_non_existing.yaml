# Official master12 case (chapter TBD; NATURAL family id — see REQUIREMENTS.md).
# Unrealized on ITS-REST 1.1.0 (AMB-33). SM Errors: party_id_does_not_exist.
id: I_ADMIN_SERVICE.physical_party_delete-delete_non_existing
kind: functional
component: ADMIN
sm_operation: I_ADMIN_SERVICE.physical_party_delete
capabilities: [PhysicalDeletion]
exercises: [AdminApi]
profiles: [OPTIONS]
test_purpose: "Physically deleting a non-existent Party is rejected as not found and leaves the system unchanged."
description: "Physically delete a non-existent party"
spec_refs:
  - "SM openehr_platform §I_ADMIN_SERVICE.physical_party_delete"
  - "CNF platform_test_schedule master12 §physical_party_delete"
applies: { rm: ">=1.0.2" }
guards:
  - "ITS-REST Admin API is DEVELOPMENT-stage — admin.openapi.yaml carries x-status: DEVELOPMENT (not STABLE); guarded until the Admin API stabilises"
requires: { server: empty }
flow:
  - step: 1
    call: physical_party_delete
    with: { a_party_id: "00000000-0000-0000-0000-000000000000" }
    expect: not_found
ambiguities: [AMB-33]
