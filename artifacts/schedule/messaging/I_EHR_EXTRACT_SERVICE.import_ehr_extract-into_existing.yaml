# Official master13 case (chapter TBD; NATURAL family id). The chapter cites
# I_EHR_EXTRACT.export_ehr()/export_ehr_extract(), but the vendored SM declares
# I_EHR_EXTRACT_SERVICE with export_ehrs/export_ehr_extracts/import_ehr/
# import_ehr_extract — the SM is the operation-resolution oracle, so cases
# anchor to its real operations (AMB-34).
#
# REALIZATION, re-derived first-hand 2026-07-29 (#659): ITS-REST 1.1.0 still
# publishes no MESSAGE / EHR-Extract / TDD API in either the docs text or the
# OAS (register AMB-34) — but this product SERVES the operation on a declared
# extension route under /message/, so the case is EXECUTED against that route
# instead of resolving excused. It gates the CAPABILITY verdicts named below
# only; no openEHR profile tier and no ITS-REST wire-conformance claim rests
# on it.
id: I_EHR_EXTRACT_SERVICE.import_ehr_extract-into_existing
kind: functional
component: MESSAGING
sm_operation: I_EHR_EXTRACT_SERVICE.import_ehr_extract
capabilities: [EhrExtract]
profiles: [OPTIONS]
test_purpose: "Importing an EXTRACT into an existing EHR adds its content as new versioned objects of that EHR."
description: "Import an EXTRACT into an existing EHR"
spec_refs:
  - "SM openehr_platform §I_EHR_EXTRACT_SERVICE.import_ehr_extract"
  - "RM common master06-change_control §Copying"
  - "RM ehr master04-ehr §Folders"
  - "CNF platform_test_schedule master13 §import_ehr_extract (chapter: export_ehr_extract)"
applies: { rm: ">=1.0.2" }
guards:
  - "EXTENSION ROUTE — no openEHR spec governs the /message/* routes; our own design/extension, declared in vocab/wire_surface.yaml served_extensions (family message-extract) and adjudicated by register AMB-34. The row gates the CAPABILITY verdicts only, never ITS-REST wire conformance"
# The extract carries a FOLDER hierarchy, not the EHR_STATUS the whole-EHR
# fixtures carry, and that is the Copying-Case-2 shape rather than a
# convenience: an EHR holds AT MOST ONE EHR_STATUS (`EHR.ehr_status` 1..1, RM
# ehr master04), so an extract bearing a second one under a different object id
# could only ever be refused — while `EHR.folders` is unbounded (§Folders), so a
# received hierarchy genuinely lands.
requires: { ehr: { commits: any } }        # mints ${ehr_id}
data_sets: [cnf.messaging.ehr_extract.folder]
flow:
  - step: 1
    call: import_ehr_extract
    with:
      an_ehr_id: "${ehr_id}"
      an_extract: "${ds:cnf.messaging.ehr_extract.folder}"
    expect: updated
ambiguities: [AMB-34]
