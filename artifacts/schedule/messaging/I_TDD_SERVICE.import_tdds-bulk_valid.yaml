# Official master13 case (chapter TBD; NATURAL family id). Anchored to the
# vendored SM I_TDD_SERVICE (i_tdd_service.adoc). ITS-REST 1.1.0 exposes NO TDD
# API, so the its-rest binding is unrealized (AMB-34) and this case is
# not-applicable with citation until a MESSAGE/TDD API release lands.
id: I_TDD_SERVICE.import_tdds-bulk_valid
kind: functional
component: MESSAGING
sm_operation: I_TDD_SERVICE.import_tdds
capabilities: [Tds]
profiles: [OPTIONS]
test_purpose: "Bulk-importing a set of well-formed TDDs is accepted and all their content is committed."
description: "Bulk-import valid TDDs"
spec_refs:
  - "SM openehr_platform §I_TDD_SERVICE.import_tdds"
  - "CNF platform_test_schedule master13 §import_tdds"
applies: { rm: ">=1.0.2" }
requires: { ehr: { commits: none } }
data_sets: [cnf.messaging.tdd.v1]
flow:
  - step: 1
    call: import_tdds
    expect: created
ambiguities: [AMB-34]
