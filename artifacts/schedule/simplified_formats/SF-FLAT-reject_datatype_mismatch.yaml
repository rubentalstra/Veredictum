# Reject rule: datatype mismatch (simplified_formats master04 §Validation:
# "Data types match expected types from the Operational Template"). A FLAT
# commit supplying a non-numeric value for a DV_QUANTITY |magnitude (a String
# where the WT input type is DECIMAL) violates the template's data-type
# constraint and is rejected. Simplified Formats is OPTIONS, so this never gates
# CORE/STANDARD.
#
# WHICH BRANCH: the released OAS assigns it — responses/422.yaml names this
# very trigger ("the underlying template … is not validating the supplied
# resource"), declared on the composition create/update the case drives, and
# master04 §Validation states every listed check against the Operational
# Template (docs text silent on the status, so the OAS grounds it).
id: SF-FLAT-reject_datatype_mismatch
kind: functional
component: SIMPLIFIED_FORMATS
sm_operation: I_EHR_COMPOSITION.create_composition
capabilities: [SimplifiedFormats]
profiles: [OPTIONS]           # SHOULD-level per ITS-REST — never gates CORE/STANDARD
test_purpose: >-
  A FLAT commit whose DV_QUANTITY |magnitude carries a non-numeric string is
  rejected: the value does not match the DECIMAL data type from the OPT.
description: "FLAT commit with a datatype mismatch on |magnitude is rejected"
spec_refs:
  - "ITS-REST simplified_formats master04 §Validation"
  - "ITS-REST simplified_formats master05 §DV_QUANTITY"
  - "ITS-REST OAS responses/422.yaml"
  - "ITS-REST overview Resources §Simplified Formats"
  - "ITS-REST overview Requests_and_responses §HTTP status codes"
  - "RM data_types §DV_QUANTITY"
applies: { rm: ">=1.0.2", its_rest: ">=1.1.0" }   # Simplified-Format media types on the resource endpoints arrived in Release-1.1.0 — ITS-REST overview Amendment_record.md §Release-1.1.0 (SPECITS-61; SPECITS-84 for CONTRIBUTION)
requires:
  server: any
  templates: [cnf.opt.vitals]
  ehr: { commits: none }                 # mints ${ehr_id}
data_sets: [cnf.sf.flat.datatype_mismatch]
flow:
  - step: 1
    call: create_composition
    format: wt-flat                       # binding adds openehr-template-id
    with: { ehr_id: "${ehr_id}", composition: "${ds:cnf.sf.flat.datatype_mismatch}" }
    expect: validation_failed             # create_composition binding: 422
    assert:
      - { assert: message_exemplar, text: "value does not match the expected data type for the field" }
