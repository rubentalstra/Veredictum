# The official multi-version combination C (master08 §Combinations of data
# sets: "Creating two valid, complete and mixed category COMPOSITIONs in one
# commit should be accepted"): one CONTRIBUTION bundling a valid event member
# and a valid persistent member, both change_type = creation.
id: I_EHR_CONTRIBUTION.commit_contribution-multiple_valid_mixed_compositions
kind: functional
component: EHR_CONTRIBUTION
sm_operation: I_EHR_CONTRIBUTION.commit_contribution
capabilities: [ChangeSets]
exercises: [Versioning]
profiles: [CORE]
test_purpose: >-
  A CONTRIBUTION bundling one valid event COMPOSITION and one valid persistent
  COMPOSITION (both change_type = creation) is accepted; both VERSIONs are
  created in the one CONTRIBUTION.
description: "One commit, mixed-category valid COMPOSITIONs — accepted (combination C)"
spec_refs:
  - "SM openehr_platform §I_EHR_CONTRIBUTION.commit_contribution"
  - "CNF platform_test_schedule master08 §Combinations of data sets (multiple versions, combination C)"
  - "RM common §change_control (VERSION.commit_audit.change_type)"
applies: { rm: ">=1.0.2" }
requires:
  server: any
  templates: [cnf.opt.minimal_event, cnf.opt.minimal_persistent]
  ehr: { commits: none }                 # mints ${ehr_id}
data_sets: [cnf.composition.minimal_event.v1, cnf.composition.persistent.v1]
flow:
  - step: 1
    call: commit_contribution
    with:
      ehr_id: "${ehr_id}"
      versions:                          # mixed categories: event + persistent, both creations
        - { data: "${ds:cnf.composition.minimal_event.v1}", change_type: creation }
        - { data: "${ds:cnf.composition.persistent.v1}", change_type: creation }
    expect: created
    capture:
      contribution_uid: created.contribution_uid
      version_uids: created.version_uids[]
    assert:
      - { assert: version, for_each: "${version_uids}", change_type: CREATE, uid_pattern: "<uuid>::<system>::1" }
postconditions:
  - { assert: version, count: 2 }        # two new VERSIONed objects, one CONTRIBUTION
