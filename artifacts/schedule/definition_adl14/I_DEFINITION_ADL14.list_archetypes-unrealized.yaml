# SM I_DEFINITION_ADL14 archetype provisioning: ITS-REST 1.1.0 surfaces no
# ADL 1.4 archetype wire (AMB-41), so the its-rest binding declares the
# operation unrealized and this case is not-applicable WITH CITATION; it
# activates when ADL 1.4 archetype provisioning lands in a later ITS release.
id: I_DEFINITION_ADL14.list_archetypes-unrealized
kind: functional
component: DEFINITION_ADL14
sm_operation: I_DEFINITION_ADL14.list_archetypes
capabilities: [Adl14ArchetypeProvisioning]
profiles: [CORE]
test_purpose: "Listing ADL 1.4 archetypes returns the loaded archetypes."
description: "list all ADL 1.4 archetypes"
spec_refs:
  - "SM openehr_platform §I_DEFINITION_ADL14 (archetype provisioning)"
  - "CNF profiles master03-profiles.adoc §Functional (Definitions: ADL 1.4 Archetype provisioning)"
applies: { rm: ">=1.0.2" }
requires: { server: empty }
flow:
  - step: 1
    call: list_archetypes
    expect: ok
ambiguities: [AMB-41]
