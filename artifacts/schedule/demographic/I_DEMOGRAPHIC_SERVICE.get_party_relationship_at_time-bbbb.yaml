# master10 case "bbbb" under I_DEMOGRAPHIC_SERVICE.get_party_relationship_at_time()
# — the schedule entry is TBD; authored as the primary declared-error path from
# the SM operation semantics (.Errors: versioned_object_does_not_exist).
# Resolvable SM operation is I_PARTY_RELATIONSHIP.get_party_relationship_at_time
# (the AMB-13 rule). ITS-REST 1.1.0 surfaces no PARTY_RELATIONSHIP resource —
# unrealized binding (proposed AMB-32); not-applicable on ITS-REST 1.1.0. (See
# REQUIREMENTS.md.)
id: I_DEMOGRAPHIC_SERVICE.get_party_relationship_at_time-bbbb
kind: functional
component: DEMOGRAPHIC
sm_operation: I_PARTY_RELATIONSHIP.get_party_relationship_at_time
capabilities: [PartyRelationshipOperations]
profiles: [OPTIONS]
guards:
  - "DEMOGRAPHIC API is DEVELOPMENT-stage in ITS-REST 1.1.0 — CNF profiles (Demographic Persistence, OPTIONS); ITS-REST demographic (DEVELOPMENT lifecycle)"
  - "master10 (DEMOGRAPHIC) is a schedule stub — every test case is TBD (aaaa/bbbb); this case is authored from the SM operation semantics (declared meaning + Errors)"
  - "PARTY_RELATIONSHIP has no ITS-REST 1.1.0 wire — not-applicable via the unrealized binding (proposed AMB-32; see REQUIREMENTS.md)"
test_purpose: >-
  get_party_relationship_at_time for an unknown versioned PARTY_RELATIONSHIP id is
  rejected (versioned_object_does_not_exist).
description: "Get a non-existent PARTY_RELATIONSHIP at time (not found)"
spec_refs:
  - "SM openehr_platform §I_PARTY_RELATIONSHIP.get_party_relationship_at_time"
  - "CNF platform_test_schedule master10 §get_party_relationship_at_time"
  - "RM demographic §PARTY_RELATIONSHIP"
applies: { rm: ">=1.0.2" }
requires: { server: empty }
flow:
  - step: 1
    call: get_party_relationship_at_time
    with:
      versioned_object_uid: "00000000-0000-4000-8000-0000000000ff"
      version_at_time: "2026-07-21T00:00:00Z"
    expect: not_found
