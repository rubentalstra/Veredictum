# DEMOGRAPHIC ARCHETYPE VALIDATION — the ACTOR arm of the same cardinality
# family: RM demographic UML/classes/org.openehr.rm.demographic.actor.adoc
# §Invariants, `Roles_valid: roles /= Void implies not roles.is_empty`. The
# attribute (0..1 List<PARTY_REF>) is declared on ACTOR, not PARTY, so this
# rule binds the ACTOR subtypes (PERSON / ORGANISATION / GROUP / AGENT) and
# NOT ROLE — a distinct invariant on a distinct class, hence its own case
# beside the PARTY.Contacts_valid one.
#
# DECISIVE DOCS TEXT for the wire (ITS-REST specifications/docs/overview/
# Requests_and_responses.md §HTTP status codes, the 422 row verbatim): "The
# request was well-formed but was unable to be followed due to semantic
# errors."
id: I_DEMOGRAPHIC_SERVICE.create_party-roles_present_empty
kind: functional
component: DEMOGRAPHIC
sm_operation: I_DEMOGRAPHIC_SERVICE.create_party
capabilities: [DemographicArchetypeValidation]
profiles: [OPTIONS]
guards:
  - "DEMOGRAPHIC API is DEVELOPMENT-stage in ITS-REST 1.1.0 — CNF profiles (Demographic Persistence, OPTIONS); ITS-REST demographic (DEVELOPMENT lifecycle)"
  - "authored from the RM demographic class model and the released ITS-REST overview status-code table; master10 (DEMOGRAPHIC) is a stalled schedule stub — a coverage guide only, not the authority"
test_purpose: >-
  A committed ACTOR whose optional roles list is present but empty is refused
  as a semantic error (ACTOR.Roles_valid).
description: "Commit a PERSON with an empty roles list (422)"
spec_refs:
  - "RM demographic §ACTOR (Roles_valid)"
  - "ITS-REST overview Requests_and_responses §HTTP status codes"
  - "SM openehr_platform §I_DEMOGRAPHIC_SERVICE.create_party"
  - "CNF profiles master03-profiles.adoc §Functional (Demographic Persistence: Archetype validation)"
applies: { rm: ">=1.0.2", its_rest: ">=1.1.0" }   # the Demographic API arrived in Release-1.1.0 — ITS-REST overview Amendment_record.md §Release-1.1.0 (SPECITS-70)
requires: { server: any }
data_sets: [cnf.demographic.person.roles_empty]
flow:
  - step: 1
    call: create_party
    with: { party: "${ds:cnf.demographic.person.roles_empty}" }
    expect: validation_failed            # overview §HTTP status codes, the 422 row
