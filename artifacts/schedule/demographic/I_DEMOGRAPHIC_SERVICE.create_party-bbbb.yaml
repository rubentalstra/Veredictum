# master10 case "bbbb" under I_DEMOGRAPHIC_SERVICE.create_party() — the schedule
# entry is TBD; authored as the primary declared-error path (content_invalid) from
# the SM operation semantics (.Errors: definition_unknown, content_invalid).
id: I_DEMOGRAPHIC_SERVICE.create_party-bbbb
kind: functional
component: DEMOGRAPHIC
sm_operation: I_DEMOGRAPHIC_SERVICE.create_party
capabilities: [PartyOperations]
profiles: [OPTIONS]
guards:
  - "DEMOGRAPHIC API is DEVELOPMENT-stage in ITS-REST 1.1.0 — CNF profiles (Demographic Persistence, OPTIONS); ITS-REST demographic (DEVELOPMENT lifecycle)"
  - "master10 (DEMOGRAPHIC) is a schedule stub — every test case is TBD (aaaa/bbbb); this case is authored from the SM operation semantics (declared meaning + Errors)"
test_purpose: >-
  Creating a PARTY (a PERSON) from an invalid data set (no PARTY_IDENTITY) is
  rejected — the create_party Pre_content_valid precondition fails
  (content_invalid), and no PARTY is created.
description: "Create a new PARTY (content_invalid)"
spec_refs:
  - "SM openehr_platform §I_DEMOGRAPHIC_SERVICE.create_party"
  - "CNF platform_test_schedule master10 §create_party"
  - "RM demographic §PARTY"
applies: { rm: ">=1.0.2" }
requires: { server: empty }
data_sets: [cnf.demographic.person.invalid]
flow:
  - step: 1
    call: create_party
    with: { party: "${ds:cnf.demographic.person.invalid}" }
    expect: validation_failed
