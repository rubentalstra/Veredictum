# The GROUP tag DELETE is KEY-scoped, not collection-scoped: it removes the
# tags under the addressed key and leaves the rest of the collection intact.
#
# GROUND: the route itself carries the key — DELETE
# /demographic/group/{uid_based_id}/tags/{key} — and the released description
# says "Deletes the ITEM_TAG resource(s) identified by `tag_key`" (ITS-REST
# operations/group_tags_delete.yaml; the parameter is spelled `{key}` in the
# path and "tag_key" in the prose — the naming mismatch is registered in
# AMB-97 and does not affect the wire). The success code is
# responses/204_deleted.yaml, "`204 No Content` is returned when the resource
# identified by the request parameters has been (logically) deleted", whose
# body floor is the status table's 204 row: "there is no additional content to
# send in the response payload body". The PUT that seeds the collection is the
# released full-collection replace (group_tags_update.yaml), and the read-back
# is group_tags_get.yaml's 200 — "`200 OK` is returned when the requested
# ITEM_TAG list is successfully retrieved".
#
# ANCHOR: `I_ITS_REST_ITEM_TAGS` is a reserved catalogue pseudo-interface
# (register AMB-161), not an SM operation — the vendored SM models no tag
# concept at all. It is pinned in NON_SM_REST_OPERATIONS
# (tools/cnf-runner/src/validate.rs), so the operation is enumerated by Axis 1
# and held to the same coverage obligation as an SM one.
#
# PER-FAMILY, NOT REDUNDANT: the five demographic party tag families are five
# SEPARATE released operations over five separate routes, each with its own
# target-kind guard (register AMB-91 makes a wrong-kind uid a 404 on every one
# of them), so a case driven on PERSON leaves the other four families
# unexercised. This case is one of the battery that retires this family's
# `coverage_gap` rows in vocab/wire_surface.yaml — coverage ratchets up.
#
# COMPONENT is taxonomy only: the CNF Platform Test Schedule has no ITEM_TAG
# chapter, so the case takes the chapter of the resource it tags.
#
# TIER: OPTIONS, honestly — the release states tag support is optional
# outright ("If the server does not support ITEM_TAGs, these headers will also
# be unsupported", ITS-REST overview Requests_and_responses.md
# §openehr-item-tag and openehr-version-item-tag), so the `ItemTags` capability
# may never gate CORE or STANDARD.
id: I_ITS_REST_ITEM_TAGS.group_tags_delete-key_scoped
kind: functional
component: DEMOGRAPHIC
sm_operation: I_ITS_REST_ITEM_TAGS.group_tags_delete
capabilities: [ItemTags]
exercises: [DemographicApi, PartyOperations]
profiles: [OPTIONS]
guards:
  - "DEMOGRAPHIC API is DEVELOPMENT-stage in ITS-REST 1.1.0 — CNF profiles (Demographic Persistence, OPTIONS); ITS-REST demographic (DEVELOPMENT lifecycle)"
  - "authored from the released ITEM_TAG operation files + RM common master07-tags.adoc; CNF master10 (DEMOGRAPHIC) is a stalled schedule stub — a coverage guide only, not the authority"
test_purpose: >-
  Deleting one tag key from a GROUP's collection removes only that key's
  tags and leaves the others readable.
description: "GROUP tag DELETE is key-scoped"
spec_refs:
  - "ITS-REST group_tags_delete.yaml §description"
  - "ITS-REST group_tags_update.yaml §description"
  - "ITS-REST group_tags_get.yaml §description"
  - "ITS-REST overview Requests_and_responses §HTTP status codes"
applies: { rm: ">=1.0.2", its_rest: ">=1.1.0" }   # the Demographic API arrived in Release-1.1.0 — ITS-REST overview Amendment_record.md §Release-1.1.0 (SPECITS-70)
formats: [canonical-json]
requires: { server: any }
data_sets: [cnf.demographic.group.v1]
flow:
  - step: 1
    call: I_DEMOGRAPHIC_SERVICE.create_party
    variant: group
    with: { party: "${ds:cnf.demographic.group.v1}" }
    expect: created
    capture:
      versioned_object_uid: created.versioned_object_uid
  - step: 2
    call: I_ITS_REST_ITEM_TAGS.group_tags_update
    with:
      uid_based_id: "${versioned_object_uid}"
      tags:
        - { key: "alpha", value: "a" }
        - { key: "beta", value: "b" }
    expect: updated
  - step: 3
    call: group_tags_delete
    with: { uid_based_id: "${versioned_object_uid}", key: "alpha" }
    expect: deleted
  - step: 4
    call: I_ITS_REST_ITEM_TAGS.group_tags_get
    with: { uid_based_id: "${versioned_object_uid}" }
    expect: ok
    assert:
      - { assert: field, path: "[0]/key", equals: "beta" }
      - { assert: field, path: "[1]", absent: true }
