# master10 case "aaaa" under I_DEMOGRAPHIC_SERVICE.get_party_relationship() — the
# schedule entry is TBD; authored as the success/happy path from the SM operation
# semantics. Resolvable SM operation is I_PARTY_RELATIONSHIP.get_party_relationship
# (the AMB-13 rule). ITS-REST 1.1.0 surfaces no PARTY_RELATIONSHIP resource, so
# the binding is unrealized (proposed AMB-32) and this case is not-applicable on
# ITS-REST 1.1.0 — the "existing relationship" precondition is unprovisionable
# over the wire until a later ITS release adds it. (See REQUIREMENTS.md.)
id: I_DEMOGRAPHIC_SERVICE.get_party_relationship-aaaa
kind: functional
component: DEMOGRAPHIC
sm_operation: I_PARTY_RELATIONSHIP.get_party_relationship
capabilities: [PartyRelationshipOperations]
profiles: [OPTIONS]
guards:
  - "DEMOGRAPHIC API is DEVELOPMENT-stage in ITS-REST 1.1.0 — CNF profiles (Demographic Persistence, OPTIONS); ITS-REST demographic (DEVELOPMENT lifecycle)"
  - "master10 (DEMOGRAPHIC) is a schedule stub — every test case is TBD (aaaa/bbbb); this case is authored from the SM operation semantics (declared meaning + Errors)"
  - "PARTY_RELATIONSHIP has no ITS-REST 1.1.0 wire — not-applicable via the unrealized binding (proposed AMB-32; see REQUIREMENTS.md)"
test_purpose: >-
  get_party_relationship for an existing versioned PARTY_RELATIONSHIP returns its
  current version.
description: "Get an existing PARTY_RELATIONSHIP (success)"
spec_refs:
  - "SM openehr_platform §I_PARTY_RELATIONSHIP.get_party_relationship"
  - "CNF platform_test_schedule master10 §get_party_relationship"
  - "RM demographic §PARTY_RELATIONSHIP"
applies: { rm: ">=1.0.2" }
requires: { server: any }
flow:
  - step: 1
    call: get_party_relationship
    with: { versioned_object_uid: "11111111-1111-4111-8111-111111111111" }
    expect: ok
