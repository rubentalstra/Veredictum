# A present-but-empty `CONTACT.addresses` is refused on the ORGANISATION create route.
#
# WHAT MAKES THE PAYLOAD INVALID (one defect, so a red row names one thing):
# RM `UML/classes/org.openehr.rm.demographic.contact.adoc` §Attributes types
# `CONTACT.addresses: List<ADDRESS>` at 1..1 — a MANDATORY list. The class
# declares no non-empty invariant of its own; the mandatory multiplicity is
# the whole bound, so a present-but-EMPTY list is the one shape the bound
# forbids — the way to say "none" is to omit an optional container, never
# to send [].
#
# WHICH BRANCH: a mandatory list cannot be constructed empty, so the body
# never converts to the resource. ITS-REST `responses/400.yaml` scopes 400 to
# "syntactically invalid ... content"; `responses/422.yaml` scopes 422 to
# content that "could be converted to a resource" and then fails semantic
# validation. This body cannot be converted — the 400 row.
#
# FIVE WIRE SURFACES, NOT ONE: the release routes each concrete party
# subtype to its own path (`/demographic/organisation`), so each carries the
# refusal independently — a server could run the strict reader on one
# route and not another.
#
# The ACCEPTING twin is `cnf.demographic.person.archetyped_full`, whose CONTACT
# carries a populated `addresses` list and is committed by
# create_party-archetyped_content_accepted.
id: I_DEMOGRAPHIC_SERVICE.create_party-organisation_contact_addresses_empty
kind: functional
component: DEMOGRAPHIC
sm_operation: I_DEMOGRAPHIC_SERVICE.create_party
capabilities: [PartyOperations, DemographicApi]
profiles: [OPTIONS]
guards:
  - "DEMOGRAPHIC API is DEVELOPMENT-stage in ITS-REST 1.1.0 — CNF profiles (Demographic Persistence, OPTIONS); ITS-REST demographic (DEVELOPMENT lifecycle)"
  - "authored from the RM demographic class model and the released ITS-REST overview status-code table; master10 (DEMOGRAPHIC) is a stalled schedule stub — a coverage guide only, not the authority"
test_purpose: >-
  A ORGANISATION whose CONTACT.addresses list is present but empty is refused as invalid content, and no party is created.
description: "ORGANISATION with an empty addresses list refused (400)"
spec_refs:
  - "RM demographic §CONTACT (addresses: List<ADDRESS>, 1..1)"
  - "ITS-REST OAS responses/400.yaml"
  - "ITS-REST OAS responses/422.yaml"
  - "ITS-REST overview Requests_and_responses §HTTP status codes"
applies: { rm: ">=1.0.2", its_rest: ">=1.1.0" }   # the Demographic API arrived in Release-1.1.0 — ITS-REST overview Amendment_record.md §Release-1.1.0 (SPECITS-70)
requires: { server: any }
data_sets: [cnf.demographic.organisation.contact_addresses_empty]
flow:
  - step: 1
    call: create_party
    variant: organisation
    with: { party: "${ds:cnf.demographic.organisation.contact_addresses_empty}" }
    expect: bad_request
