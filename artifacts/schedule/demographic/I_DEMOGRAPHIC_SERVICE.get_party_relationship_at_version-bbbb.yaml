# master10 case "bbbb" under
# I_DEMOGRAPHIC_SERVICE.get_party_relationship_at_version() — the schedule entry is
# TBD; authored as the primary declared-error path from the SM operation semantics
# (.Errors: object_version_does_not_exist). Resolvable SM operation is
# I_PARTY_RELATIONSHIP.get_party_relationship_at_version.
# ITS-REST 1.1.0 surfaces no PARTY_RELATIONSHIP resource — unrealized binding
# (AMB-32); not-applicable on ITS-REST 1.1.0.
id: I_DEMOGRAPHIC_SERVICE.get_party_relationship_at_version-bbbb
kind: functional
component: DEMOGRAPHIC
sm_operation: I_PARTY_RELATIONSHIP.get_party_relationship_at_version
capabilities: [PartyRelationshipOperations]
profiles: [OPTIONS]
guards:
  - "DEMOGRAPHIC API is DEVELOPMENT-stage in ITS-REST 1.1.0 — CNF profiles (Demographic Persistence, OPTIONS); ITS-REST demographic (DEVELOPMENT lifecycle)"
  - "authored from SM I_DEMOGRAPHIC_SERVICE / I_PARTY_RELATIONSHIP operation semantics (declared meaning + Errors) + released ITS-REST demographic docs; master10 (DEMOGRAPHIC) is a stalled schedule stub (all cases TBD aaaa/bbbb) — a coverage guide only, not the authority"
  - "PARTY_RELATIONSHIP has no ITS-REST 1.1.0 wire — not-applicable via the unrealized binding (AMB-32)"
test_purpose: >-
  get_party_relationship_at_version for an unknown PARTY_RELATIONSHIP version id is
  rejected (object_version_does_not_exist).
description: "Get a non-existent PARTY_RELATIONSHIP version (not found)"
spec_refs:
  - "SM openehr_platform §I_PARTY_RELATIONSHIP.get_party_relationship_at_version"
  - "CNF platform_test_schedule master10 §get_party_relationship_at_version"
  - "RM demographic §PARTY_RELATIONSHIP"
applies: { rm: ">=1.0.2" }
requires: { server: empty }
flow:
  - step: 1
    call: get_party_relationship_at_version
    with: { version_uid: "00000000-0000-4000-8000-0000000000ff::openEHRSys.example.com::1" }
    expect: not_found
