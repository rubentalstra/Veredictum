# The CROSS-SPACE direction of the route-kind rule, from the demographic side:
# a COMPOSITION container uid presented on a PARTY tag route is a miss.
#
# GROUND: RM common master06-change_control_package.adoc §Typing makes a version
# container single-typed ("all versions in a given `VERSIONED_OBJECT` are of the
# same type, such as `COMPOSITION`, `FOLDER`, or `PARTY`"), so no VERSIONED_PARTY
# carries that uid. The released 404 covers it verbatim: "`404 Not Found` is
# returned when the `uid_based_id` does not exist" (ITS-REST
# responses/404_unknown_uid_based_id.yaml). Register AMB-91 adjudicates the
# cross-space direction explicitly — "a COMPOSITION uid presented on a party tag
# route, or a party uid on a composition tag route" are the same miss.
#
# ANCHOR: `I_ITS_REST_ITEM_TAGS` is a reserved catalogue pseudo-interface
# (register AMB-161), not an SM operation — the vendored SM models no tag
# concept at all. It is pinned in NON_SM_REST_OPERATIONS
# (tools/cnf-runner/src/validate.rs), so the operation is enumerated by Axis 1
# and held to the same coverage obligation as an SM one.
#
# COMPONENT is taxonomy only: the CNF Platform Test Schedule has no ITEM_TAG
# chapter, so the case takes the chapter of the resource it tags.
#
# TIER: OPTIONS, honestly — the release states tag support is optional outright
# ("If the server does not support ITEM_TAGs, these headers will also be
# unsupported", ITS-REST overview Requests_and_responses.md §openehr-item-tag
# and openehr-version-item-tag), so the `ItemTags` capability may never gate
# CORE or STANDARD.
id: I_ITS_REST_ITEM_TAGS.person_tags_get-cross_space_composition_uid
kind: functional
component: DEMOGRAPHIC
sm_operation: I_ITS_REST_ITEM_TAGS.person_tags_get
capabilities: [ItemTags]
exercises: [DemographicApi, EhrApi]
profiles: [OPTIONS]
ambiguities: [AMB-91]
guards:
  - "DEMOGRAPHIC API is DEVELOPMENT-stage in ITS-REST 1.1.0 — CNF profiles (Demographic Persistence, OPTIONS); ITS-REST demographic (DEVELOPMENT lifecycle)"
  - "authored from the released ITEM_TAG operation files + RM common master07-tags.adoc; CNF master10 (DEMOGRAPHIC) is a stalled schedule stub — a coverage guide only, not the authority"
test_purpose: >-
  A COMPOSITION container uid presented on the PERSON tag route is a miss, and
  the COMPOSITION's own tag route still answers.
description: "PERSON tag route rejects an EHR-space uid (404)"
spec_refs:
  - "RM common §change_control (master06 §Typing)"
  - "ITS-REST overview Requests_and_responses §HTTP status codes"
  - "ITS-REST person_tags_get.yaml §description"
applies: { rm: ">=1.0.2", its_rest: ">=1.1.0" }   # the Demographic API arrived in Release-1.1.0 — ITS-REST overview Amendment_record.md §Release-1.1.0 (SPECITS-70)
formats: [canonical-json]
requires:
  server: any
  templates: [cnf.opt.minimal_event]
  ehr: { commits: none }
data_sets: [cnf.composition.minimal_event.v1]
flow:
  - step: 1
    call: I_EHR_COMPOSITION.create_composition
    with: { ehr_id: "${ehr_id}", composition: "${ds:cnf.composition.minimal_event.v1}" }
    expect: created
    capture:
      versioned_object_uid: created.versioned_object_uid
  - step: 2
    call: person_tags_get
    with: { uid_based_id: "${versioned_object_uid}" }
    expect: not_found
  - step: 3                                       # the COMPOSITION's own tag route is unaffected
    call: I_ITS_REST_ITEM_TAGS.composition_tags_get
    with: { ehr_id: "${ehr_id}", uid_based_id: "${versioned_object_uid}" }
    expect: ok
    assert:
      - { assert: returns, equals: [] }
