# The canonical-XML REQUEST-payload branch of update_party: the same second
# PERSON version as -aaaa, with the new PARTY serialized as canonical XML.
#
# Decisive released sentences (ITS-REST overview `Resources.md` §"XML Format"):
# "When resources are serialized in **canonical XML** format, both request
# payloads and responses MUST conform to the [published XSDs]" and "A client MAY
# use the header `Content-Type: application/xml` in the requests to specify the
# XML payload format." The released OAS corroborates the type is on offer for
# this operation: `operations/person_update.yaml` references
# `parameters/header/ContentType_LOCATABLE.yaml`, whose enum contains
# `application/xml`. The `preceding_version_uid` realization is unchanged by the
# payload format (overview `Requests_and_responses.md` §"If-Match and
# accidental overwrites").
#
# The precondition version is committed in canonical JSON so that exactly ONE
# behaviour — the XML update payload — is under test on this row.
#
# §"Data representation" leaves XML support to the service, hence the
# `canonical-xml` format axis.
id: I_DEMOGRAPHIC_SERVICE.update_party-xml
kind: functional
component: DEMOGRAPHIC
sm_operation: I_PARTY.update_party
capabilities: [PartyOperations]
profiles: [OPTIONS]
formats: [canonical-xml]               # tech-profile axis: XML-less parties are not judged by this row
guards:
  - "DEMOGRAPHIC API is DEVELOPMENT-stage in ITS-REST 1.1.0 — CNF profiles (Demographic Persistence, OPTIONS); ITS-REST demographic (DEVELOPMENT lifecycle)"
  - "authored from SM I_DEMOGRAPHIC_SERVICE / I_PARTY operation semantics (declared meaning + Errors) + released ITS-REST demographic docs; master10 (DEMOGRAPHIC) is a stalled schedule stub (all cases TBD aaaa/bbbb) — a coverage guide only, not the authority"
test_purpose: >-
  update_party with a canonical-XML PERSON request payload and the correct
  preceding_version_uid creates a second VERSION whose stored content equals
  the canonical-JSON twin of the same PERSON.
description: "Update a PERSON from a canonical-XML payload"
spec_refs:
  - "SM openehr_platform §I_PARTY.update_party"
  - "ITS-REST overview Resources.md §XML Format"
  - "ITS-REST overview Resources.md §Data representation"
  - "ITS-REST overview Requests_and_responses.md §If-Match and accidental overwrites"
  - "RM demographic §PERSON"
applies: { rm: ">=1.0.2", its_rest: ">=1.1.0" }
requires: { server: empty }
data_sets: [cnf.demographic.person.v1, cnf.demographic.person.v2.xml, cnf.demographic.person.v2]
flow:
  - step: 1
    call: I_DEMOGRAPHIC_SERVICE.create_party
    format: canonical-json             # precondition only — the XML branch under test is step 2
    with: { party: "${ds:cnf.demographic.person.v1}" }
    expect: created
    capture:
      preceding_version_uid: created.version_uid
      versioned_object_uid: created.versioned_object_uid
  - step: 2
    call: update_party
    format: canonical-xml              # Content-Type: application/xml on the request body
    with:
      versioned_object_uid: "${versioned_object_uid}"
      party: "${ds:cnf.demographic.person.v2.xml}"
      preceding_version_uid: "${preceding_version_uid}"   # ITS-REST: If-Match (overview §If-Match)
    expect: updated
    capture: { v2_uid: updated.version_uid }
    assert:
      - { assert: instance_of, rm_type: PERSON }
      - { assert: version, of: "${v2_uid}", uid_pattern: "${versioned_object_uid}::<system>::2" }
      # Format changed, content did not.
      - { assert: equivalent, to: "${ds:cnf.demographic.person.v2}", ignoring: server_assigned }
postconditions:
  - { assert: version, count: 2 }
  - { assert: version, of: "${v2_uid}", change_type: MODIFY }
