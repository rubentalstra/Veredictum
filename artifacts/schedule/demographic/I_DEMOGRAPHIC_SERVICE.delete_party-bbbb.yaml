# master10 case "bbbb" under I_DEMOGRAPHIC_SERVICE.delete_party() — the schedule
# entry is TBD; authored as the primary declared-error path from the SM operation
# semantics (.Errors: versioned_object_does_not_exist). Resolvable SM operation is
# I_PARTY.delete_party.
id: I_DEMOGRAPHIC_SERVICE.delete_party-bbbb
kind: functional
component: DEMOGRAPHIC
sm_operation: I_PARTY.delete_party
capabilities: [PartyOperations]
profiles: [OPTIONS]
guards:
  - "DEMOGRAPHIC API is DEVELOPMENT-stage in ITS-REST 1.1.0 — CNF profiles (Demographic Persistence, OPTIONS); ITS-REST demographic (DEVELOPMENT lifecycle)"
  - "authored from SM I_DEMOGRAPHIC_SERVICE / I_PARTY operation semantics (declared meaning + Errors) + released ITS-REST demographic docs; master10 (DEMOGRAPHIC) is a stalled schedule stub (all cases TBD aaaa/bbbb) — a coverage guide only, not the authority"
test_purpose: >-
  delete_party for an unknown PARTY version id is rejected
  (versioned_object_does_not_exist).
description: "Delete a non-existent PARTY (not found)"
spec_refs:
  - "SM openehr_platform §I_PARTY.delete_party"
  - "CNF platform_test_schedule master10 §delete_party"
  - "RM demographic §PARTY"
applies: { rm: ">=1.0.2" }
requires: { server: empty }
flow:
  - step: 1
    call: delete_party
    with: { version_uid: "00000000-0000-4000-8000-0000000000ff::openEHRSys.example.com::1" }
    expect: not_found
