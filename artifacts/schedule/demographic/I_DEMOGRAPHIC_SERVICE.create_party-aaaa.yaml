# master10 case "aaaa" under I_DEMOGRAPHIC_SERVICE.create_party() — the schedule
# entry is TBD; authored as the success/happy path from the SM operation
# semantics. create_party is genuinely on I_DEMOGRAPHIC_SERVICE; the abstract
# PARTY is realized via the concrete PERSON endpoint (the binding NOTE is the record).
id: I_DEMOGRAPHIC_SERVICE.create_party-aaaa
kind: functional
component: DEMOGRAPHIC
sm_operation: I_DEMOGRAPHIC_SERVICE.create_party
capabilities: [PartyOperations, DemographicApi]
profiles: [OPTIONS]
guards:
  - "DEMOGRAPHIC API is DEVELOPMENT-stage in ITS-REST 1.1.0 — CNF profiles (Demographic Persistence, OPTIONS); ITS-REST demographic (DEVELOPMENT lifecycle)"
  - "authored from SM I_DEMOGRAPHIC_SERVICE / I_PARTY operation semantics (declared meaning + Errors) + released ITS-REST demographic docs; master10 (DEMOGRAPHIC) is a stalled schedule stub (all cases TBD aaaa/bbbb) — a coverage guide only, not the authority"
test_purpose: >-
  Creating the first version of a new PARTY (a PERSON) from a valid data set
  succeeds, causing server-side creation of a new VERSIONED_OBJECT,
  ORIGINAL_VERSION and CONTRIBUTION; the PARTY is then retrievable.
description: "Create a new PARTY (success)"
spec_refs:
  - "SM openehr_platform §I_DEMOGRAPHIC_SERVICE.create_party"
  - "CNF platform_test_schedule master10 §create_party"
  - "RM demographic §PERSON"
applies: { rm: ">=1.0.2" }
requires: { server: empty }
data_sets: [cnf.demographic.person.v1]
flow:
  - step: 1
    call: create_party
    with: { party: "${ds:cnf.demographic.person.v1}" }
    expect: created
postconditions:
  - assert: state
    text: "The created PERSON exists and is retrievable"
    verified_by: I_DEMOGRAPHIC_SERVICE.get_party-aaaa
verified_by: [I_DEMOGRAPHIC_SERVICE.get_party-aaaa]
