# master10 case "bbbb" under I_DEMOGRAPHIC_SERVICE.get_party_at_version() — the
# schedule entry is TBD; authored as the primary declared-error path from the SM
# operation semantics (.Errors: object_version_does_not_exist). Resolvable SM
# operation is I_PARTY.get_party_at_version (the AMB-13 rule).
id: I_DEMOGRAPHIC_SERVICE.get_party_at_version-bbbb
kind: functional
component: DEMOGRAPHIC
sm_operation: I_PARTY.get_party_at_version
capabilities: [PartyOperations]
profiles: [OPTIONS]
guards:
  - "DEMOGRAPHIC API is DEVELOPMENT-stage in ITS-REST 1.1.0 — CNF profiles (Demographic Persistence, OPTIONS); ITS-REST demographic (DEVELOPMENT lifecycle)"
  - "master10 (DEMOGRAPHIC) is a schedule stub — every test case is TBD (aaaa/bbbb); this case is authored from the SM operation semantics (declared meaning + Errors)"
test_purpose: >-
  get_party_at_version for an unknown PARTY version id is rejected
  (object_version_does_not_exist).
description: "Get a non-existent PARTY version (not found)"
spec_refs:
  - "SM openehr_platform §I_PARTY.get_party_at_version"
  - "CNF platform_test_schedule master10 §get_party_at_version"
  - "RM demographic §PARTY"
applies: { rm: ">=1.0.2" }
requires: { server: empty }
flow:
  - step: 1
    call: get_party_at_version
    with: { version_uid: "00000000-0000-4000-8000-0000000000ff::openEHRSys.example.com::1" }
    expect: not_found
