# The canonical-XML branch of the has_composition existence probe. ITS-REST
# surfaces no dedicated composition-existence probe, so the SM Boolean is
# realized as the version GET (200 = TRUE) — the same realization the
# variant-less binding records; this row asks that question with
# `Accept: application/xml`.
#
# Decisive released sentences (ITS-REST overview `Resources.md` §"XML Format"):
# "The client SHOULD use the `Accept: application/xml` request header to specify
# the expected XML response format. If the service cannot fulfill this aspect of
# the request, it MUST respond with HTTP status code `406 Not Acceptable`.
# Proper header `Content-Type: application/xml` MUST be present in the response
# of the service unless the response has no content body (HTTP status code
# `204`)." The existence answer is the STATUS, so a conformant service must not
# turn an XML `Accept` into a different existence verdict: 200 still means TRUE.
# The binding's `Content-Type: negotiated` matcher executes the media-type MUST.
#
# The body floor is the resource's own: this route serves a COMPOSITION, whose
# document root the published ITS-XML schema declares globally (RM
# documents/Composition.xsd: `<xs:element name="composition" type="COMPOSITION"/>`).
#
# §"Data representation" leaves XML support to the service, hence the
# `canonical-xml` format axis.
id: I_EHR_COMPOSITION.has_composition-xml
kind: functional
component: EHR_COMPOSITION
sm_operation: I_EHR_COMPOSITION.has_composition
capabilities: [CompositionOps]
profiles: [CORE]
formats: [canonical-xml]               # tech-profile axis: XML-less parties are not judged by this row
test_purpose: >-
  has_composition for an existing VERSION.uid still reports the composition
  exists when the probe is made with `Accept: application/xml`, answering in
  canonical XML.
description: "Has existing COMPOSITION, probed as canonical XML"
spec_refs:
  - "SM openehr_platform §I_EHR_COMPOSITION.has_composition"
  - "ITS-REST overview Resources.md §XML Format"
  - "ITS-REST overview Resources.md §Data representation"
  - "ITS-XML components §RM documents/Composition.xsd (the globally declared `composition` element)"
applies: { rm: ">=1.0.2" }
requires:
  server: any
  templates: [cnf.opt.minimal_event]
  ehr: { commits: none }               # mints ${ehr_id}
data_sets: [cnf.composition.minimal_event.v1]
flow:
  - step: 1
    call: create_composition
    format: canonical-json             # precondition only — the XML branch under test is step 2
    with: { ehr_id: "${ehr_id}", composition: "${ds:cnf.composition.minimal_event.v1}" }
    expect: created
    capture: { version_uid: created.version_uid }
  - step: 2
    call: has_composition
    format: canonical-xml              # Accept: application/xml on a bodyless read
    with: { ehr_id: "${ehr_id}", version_uid: "${version_uid}" }
    expect: ok                          # the existence answer is the status: TRUE
    assert:
      - { assert: returns, matches: '<([A-Za-z_][A-Za-z0-9_.-]*:)?composition[\s/>]' }
