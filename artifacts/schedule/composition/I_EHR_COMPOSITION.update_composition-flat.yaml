# The FLAT REQUEST-payload branch of update_composition: a second VERSION
# committed from a Simplified Flat JSON body. The commit-side twin of the FLAT
# create branch SF-FLAT-commit_roundtrip_ctx_defaults already exercises on POST.
#
# Decisive released sentences:
#  * ITS-REST overview `Resources.md` §"Simplified Formats" — clients and
#    servers "MUST use: `application/openehr.wt.flat+json` for the Simplified
#    Flat JSON format"; "A client MAY use the header `Content-Type` in the
#    requests to specify the simplified payload format."
#  * ITS-REST overview `Requests_and_responses.md` §openehr-template-id — "The
#    `openehr-template-id` request header MUST be used whenever committing
#    COMPOSITION (via `PUT` or `POST` methods) using a Simplified Format which
#    does not support TEMPLATE_ID value under an equivalent
#    `LOCATABLE.archetype_details.template_id` attribute of contained data."
#    The PUT is named outright, so the update binding declares the header
#    required on both simplified formats (the released OAS omits it from
#    `operations/composition_update.yaml` `parameters`; the docs text wins).
#  * ITS-REST overview `Requests_and_responses.md` §"If-Match and accidental
#    overwrites" — the `preceding_version_uid` realization is unchanged by the
#    payload format.
#
# Both versions are committed in FLAT so the row tests exactly one thing: that
# the update route accepts the Simplified Flat payload and versions it. The
# response is negotiated canonical JSON (the default `Accept` of a
# body-carrying step), which is what the RM assertions read.
#
# Simplified-Format support is SHOULD-level (Resources.md §"Data
# representation"), so the row rides the SimplifiedFormats capability and never
# gates CORE/STANDARD.
id: I_EHR_COMPOSITION.update_composition-flat
kind: functional
component: SIMPLIFIED_FORMATS
sm_operation: I_EHR_COMPOSITION.update_composition
capabilities: [SimplifiedFormats]
exercises: [CompositionOps, Versioning]
profiles: [OPTIONS]           # SHOULD-level per ITS-REST — never gates CORE/STANDARD
test_purpose: >-
  update_composition with a Simplified Flat JSON request payload and the
  `openehr-template-id` header creates a second VERSION with change_type
  MODIFY.
description: "Update a COMPOSITION from a FLAT payload"
spec_refs:
  - "SM openehr_platform §I_EHR_COMPOSITION.update_composition"
  - "ITS-REST overview Resources.md §Simplified Formats"
  - "ITS-REST overview Requests_and_responses.md §openehr-template-id"
  - "ITS-REST simplified_formats master02 §MIME Types"
  - "RM common §change_control (VERSION.commit_audit.change_type)"
applies: { rm: ">=1.0.2", its_rest: ">=1.1.0" }   # Simplified-Format media types on the resource endpoints arrived in Release-1.1.0 — ITS-REST overview Amendment_record.md §Release-1.1.0 (SPECITS-61; SPECITS-84 for CONTRIBUTION)
requires:
  server: any
  templates: [cnf.opt.minimal_action]
  ehr: { commits: none }               # mints ${ehr_id}
data_sets: [cnf.flat.vitals.minimal_ctx, cnf.flat.vitals.composer_self]
flow:
  - step: 1
    call: create_composition
    format: wt-flat                    # precondition: the container to update; binding adds openehr-template-id
    with: { ehr_id: "${ehr_id}", composition: "${ds:cnf.flat.vitals.minimal_ctx}" }
    expect: created
    capture:
      preceding_version_uid: created.version_uid
      versioned_object_uid: created.versioned_object_uid
  - step: 2
    call: update_composition
    format: wt-flat                    # Content-Type: application/openehr.wt.flat+json on the request body
    with:
      ehr_id: "${ehr_id}"
      composition: "${ds:cnf.flat.vitals.composer_self}"
      versioned_object_uid: "${versioned_object_uid}"
      preceding_version_uid: "${preceding_version_uid}"   # ITS-REST: If-Match (overview §If-Match)
    expect: updated
    capture: { v2_uid: updated.version_uid }
    assert:
      - { assert: instance_of, rm_type: COMPOSITION }
      - { assert: version, of: "${v2_uid}", uid_pattern: "${versioned_object_uid}::<system>::2" }
postconditions:
  - { assert: version, count: 2 }
  - { assert: version, of: "${v2_uid}", change_type: MODIFY }
