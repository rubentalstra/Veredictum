# Seeded defect: an empty wire-surface register. Removing the authored,
# spec-cited exception ledger makes every uncovered SM operation / binding
# branch a surface-coverage finding again — proving the ledger is
# load-bearing and coverage can only ratchet up (issue FerroEHR#271; the
# coverage mandate: silence is never coverage).
sm_operations: []
branches: []
elements: []
