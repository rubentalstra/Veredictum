id: DEFECT-signature-no-fact
kind: functional
component: SECURITY
sm_operation: I_EHR_SERVICE.create_ehr
capabilities: [EhrOperations]
profiles: [CORE]
test_purpose: "seeded defect: signature assertion carrying no fact"
description: "seeded defect"
spec_refs: ["CNF platform_test_schedule master06 §create_ehr data sets"]
requires: { server: empty }
flow:
  - step: 1
    call: create_ehr
    expect: created
    capture: { v: created.version_uid }
postconditions:
  - { assert: signature, of: "${v}" }
