# The seeded id MUST stay outside the register's allocated range — an id the
# register later allocates for real stops seeding the defect and silently
# disarms this gate (which is how AMB-99 stopped working once it was written).
id: DEFECT-unresolved-ambiguity
kind: functional
component: EHR
sm_operation: I_EHR_SERVICE.create_ehr
capabilities: [EhrOperations]
profiles: [CORE]
test_purpose: "seeded defect: ambiguity id absent from the register"
description: "seeded defect"
spec_refs: ["CNF platform_test_schedule master06 §create_ehr data sets"]
requires: { server: empty }
flow:
  - step: 1
    call: create_ehr
    expect: created
ambiguities: [AMB-9999]
